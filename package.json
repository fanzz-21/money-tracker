{
  "name": "money-tracker",
  "version": "1.0.0",
  "description": "Money tracker — catat kas harian, sync via Supabase. Static frontend (HTML/JS ESM) siap deploy ke Vercel.",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site - no build step'",
    "deploy": "vercel --prod",
    "lint": "eslint js/",
    "lint:strict": "eslint -c .eslintrc-strict.json js/",
    "smoke": "node tests/smoke-classic.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "eslint": "^8.57.1"
  }
}
